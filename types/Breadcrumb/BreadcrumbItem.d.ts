import { VComponent } from '../component';

export declare class BreadcrumbItem extends VComponent {
  /**
   * Style as the currently active section
   *
   * @default: false
   */
  active: Boolean;
}
