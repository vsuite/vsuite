import { VComponent } from '../component';

export declare class InputGroupAddon extends VComponent {
  /**
   * An Input group addon can show that it is disabled
   *
   * @default: false
   */
  disabled: boolean;
}
