import { VComponent } from '../component';

export declare class RadioGroup extends VComponent {
  /**
   * A radio group can have different appearances
   *
   * @default: 'default'
   */
  appearance: 'default' | 'picker';

  /**
   * Inline layout
   *
   * @default: false
   */
  inline: boolean;
}
